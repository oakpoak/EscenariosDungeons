using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class CharacterData
{
    // Valor inicial -1 indica que todavía no se ha asignado
    public static float[] currentHealth = { -1f, -1f, -1f };
}

